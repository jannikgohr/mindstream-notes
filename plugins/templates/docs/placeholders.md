# Placeholders

The title and body are run through the placeholder engine, so you can write
things like:

- `{{date}}`, `{{time}}`, `{{datetime}}`
- date math and formats: `{{date+7d}}`, `{{date:dddd}}`
- `{{uuid}}`, `{{title}}`
- filters: `{{title|slug}}`
