/**
 * Deprecated. Tree types moved to `$lib/api/tree`; the mock store moved
 * to `$lib/api/mock-store`. This file kept temporarily so older imports
 * still resolve while the codebase finishes migrating to the api/ layer.
 */

export type { TreeNode, FolderNode, NoteNode } from './api';
export type { NoteSummary } from './api';
